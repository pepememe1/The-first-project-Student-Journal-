# ============================================================
# requirements.txt — GradeBookAI Release 2.5 (клиент-десктоп)
# Python 3.10+
#
# Установка:  pip install -r requirements.txt
#
# Версии заданы нижней границей (>=) — заведомо рабочие и совместимые.
# ⚠️ ПЕРЕД продажей/поставкой зафиксируй ТОЧНЫЕ проверенные версии:
#    запусти на своей рабочей машине  ->  pip freeze > requirements.txt
#    Это гарантирует, что у покупателя соберётся ровно то, что у тебя.
# ============================================================

# ---- ОБЯЗАТЕЛЬНЫЕ (без них приложение не запустится) ----
# ⚠️ PySide6 (Qt) УБРАН. Окно рисует системный движок Edge через pywebview, а Qt-оболочка
# в релизный .exe не входила никогда — то есть у людей не исполнялась ни разу. Одни только
# pyside6-addons весят 168 МБ, и ставились они каждому. Исходники — WORK/GB-legacy-qt.
openpyxl>=3.1         # экспорт оценок/ведомостей в Excel
python-docx>=1.1      # экспорт журнала/ведомостей в Word (.docx) — как на вебе
requests>=2.31        # HTTP-запросы к API
# cryptography теперь обязательна: шифрование ПДн (Fernet) и защита ключей.
# Без неё приложение НЕ запускается (152-ФЗ) — слабого XOR-фолбэка больше нет.
cryptography>=42.0
# ГОСТ-этап хеша пароля (PBKDF2-Стрибог512, Р 50.1.111-2016). На dev/Windows — этот
# pure-Python пакет (MIT). На БОЕВОМ сервере ГОСТ должен считать сертифицированное
# СКЗИ (ViPNet CSP / OpenSSL GOST-engine) — тогда gostcrypto не нужен, бэкенд авто.
gostcrypto>=1.2.5

# ---- ОПЦИОНАЛЬНЫЕ (приложение запустится и без них, но функции урежутся) ----
gigachat>=0.1         # Vector через GigaChat. Без неё — офлайн-шаблоны / Ollama
#
# Синхронизация идёт через REST API-сервер (каталог server/), а не напрямую в БД.
# Полный список зависимостей самого сервера — в server/requirements.txt.
# ⚠️ Боевой сервер с ПДн должен быть в РФ (152-ФЗ ст.18 ч.5): ЛВС колледжа или
#    российское облако; канал — HTTPS/TLS.

# ---- ОБЩИЙ Vue-интерфейс (§11 CLAUDE.md, «один UI») ----
# Десктоп поднимает НАСТОЯЩЕЕ серверное приложение (server/app) у себя же на 127.0.0.1
# (desktop/local_api.py) — тем же кодом, что и боевой сервер, и показывает ту же Vue SPA
# офлайн. Без этих пакетов локальный сервер не поднимется, а вместе с ним не откроется
# и окно: нативных экранов в продукте нет с удаления Qt (13.08.2026), падать больше
# некуда — программа честно сообщает причину (см. webview2_app.fail_reason()).
#
# ⚠️ ЧЕГО ЗДЕСЬ НЕТ И ПОЧЕМУ. `sqlcipher3-binary` клиенту не объявляем: под Windows
# готового wheel нет. Но это НЕ значит, что локальная база не шифруется — с 28.08.2026
# обе локальные базы шифруются ЦЕЛИКОМ (SQLCipher, ключ устройства под DPAPI), и в
# собранный .exe драйвер кладётся сборкой. Прежняя редакция этой строки утверждала
# «локальную базу не шифруем» — на тот момент это было правдой ровно для одной таблицы
# из семи, а ФИО, группы и оценки лежали на диске открытым текстом (нашёл Ярослав).
# ⚠️ Драйвера PostgreSQL нет ни здесь, ни на сервере: база у продукта одна — SQLite
# (решение 04.09.2026, см. шапку server/app/db.py).
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
webauthn>=2.0
httpx>=0.27
#Шифрование локальной копии базы на десктопе (вшивается в .exe, см. GradeBookAI.spec)
sqlcipher3>=0.5; sys_platform == "win32"
#Оболочка окна на СИСТЕМНОМ движке WebView2 (см. ui/webview2_shell.py): позволяет
#убрать из сборки Chromium и Qt. Рантайм в Windows 10/11 уже установлен системой.
pywebview>=6.0; sys_platform == "win32"
#Дельта-обновления десктопа (data/updater.py): сжатие «относительно словаря» = патч
#между сборками. Импорт ЛЕНИВЫЙ — без пакета обновления просто пойдут полной закачкой.
zstandard>=0.22
