# ============================================================
# requirements-dev.txt — инструменты разработки (НЕ нужны для запуска приложения)
# Установка:  pip install -r requirements-dev.txt
# Конфигурация всех трёх — в pyproject.toml.
# ============================================================
ruff>=0.6          # линтер + автоформат-фиксы (быстрый, заменяет flake8/isort)
black>=24.0        # автоформат (единый стиль, конец споров о пробелах)
mypy>=1.10         # статическая проверка типов (внедряем постепенно, по модулям)
pytest>=8.0        # тесты (клиент из корня; сервер — cd server && pytest)
bandit[toml]>=1.8     # SAST — гейт на MEDIUM+ (настройки в pyproject [tool.bandit])
pip-audit>=2.9        # известные уязвимости зависимостей (база OSV)
cyclonedx-bom>=6.0    # состав поставки (SBOM) — tools/make_sbom.sh
#Сверка двух объявлений зависимостей читает pyproject.toml. В 3.11+ для этого
#есть встроенный tomllib, на 3.10 — нет, а часть машин команды ещё на 3.10.
tomli>=2.0; python_version < "3.11"
