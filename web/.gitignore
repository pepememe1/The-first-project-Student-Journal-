# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Локальные переменные окружения (не коммитим — там могут быть адреса/секреты)
.env
.env.*
!.env.example

# Android: подпись release (СЕКРЕТЫ — никогда не коммитить!) и сборочные артефакты
android/release.keystore
android/keystore.properties
android/app/build
android/build
android/.gradle
android/app/release

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
