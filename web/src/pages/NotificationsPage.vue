<script setup>
// NotificationsPage — «Уведомления» ОТДЕЛЬНЫМ разделом, сразу под «Сообщениями»
// (просьба Влада, 31.08.2026).
//
// ━━ ПОЧЕМУ ОТДЕЛЬНО ━━
// Лента жила карточкой внутри «Профиля» — то есть письма об оценках, домашних заданиях
// и мероприятиях лежали в разделе «как я выгляжу», между цветом плашки и стилем имени.
// Найти их там можно было только случайно, а приходят они каждый день. Соседство с
// «Сообщениями» честнее по существу: и там, и там входящее, разница лишь в том, пишет
// человек или система.
//
// ⚠️ НЕ ПУТАТЬ С НАСТРОЙКОЙ «Уведомления» в настройках: там переключатели «что
// присылать» (свойство аккаунта), здесь — сами письма. Названия совпадают, суть разная,
// поэтому в настройках подпись категории — «Что присылать».
import { useLocaleStore } from '@/stores/locale'
import NotificationsInbox from '@/components/NotificationsInbox.vue'

const locale = useLocaleStore()
</script>

<template>
  <!-- Ширина ограничена: список писем — это строки текста, а строку в 1800 px глаз не
       дочитывает, теряя начало следующей. Тот же предел, что у карточки профиля. -->
  <div class="mx-auto w-full max-w-4xl">
    <h1 class="mb-4 font-title text-xl font-extrabold text-text lg:text-2xl">
      {{ locale.t('nav.notifications', 'Уведомления') }}
    </h1>
    <NotificationsInbox />
  </div>
</template>
