# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1536m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

#━━ ЭТОТ ФАЙЛ ЛЕЖИТ В GIT. СЕКРЕТАМ ЗДЕСЬ НЕ МЕСТО. ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#Здесь раньше было написано «держим вне git вместе с настройками подписи» — НЕПРАВДА,
#файл отслеживается git с самого начала (проверено 21.08.2026 при разборе безопасности
#мобильной версии). Само значение ниже секретом не является и от этого не пострадало:
#project_id виден в любом распакованном APK. Опасна была именно ИНСТРУКЦИЯ: файл,
#назначенный хранить негитовые значения, — приманка, в которую следующий человек
#положит настоящий секрет, следуя написанному, и утечёт он уже по-настоящему.
#
#Куда что класть:
#  • пароли и путь к ключу подписи → web/android/keystore.properties (вне git, см.
#    .gitignore: keystore.properties, *.keystore) — их читает app/build.gradle;
#  • серверные секреты (сервисный токен RuStore Push и прочее) → server/.env на VPS,
#    в репозиторий не попадают вовсе;
#  • сюда — только несекретные значения сборки.
#Держит web/tests/androidSecrets.test.mjs.
RUSTORE_PROJECT_ID=XeK-6qHY45fXQ3okWKeE4ziVdGRI3Ta6
