<?xml version="1.0" encoding="utf-8"?>
<!--
  Средний и большой виджет: шапка + ПРОКРУЧИВАЕМЫЙ список пар.

  ⚠️ Список — настоящий ListView, а не стопка добавленных вручную строк, как было
  раньше. Причина: шрифты стали крупными (расписание смотрят мельком, а не вчитываются),
  и на низкой ячейке в экран влезает полторы пары. Единственный способ показать весь
  день крупным шрифтом — прокрутка, а прокрутка в виджете бывает только через коллекцию
  (ListView + RemoteViewsService, см. ScheduleWidgetService).

  ⚠️ RemoteViews поддерживает НЕ ВСЕ контейнеры: ConstraintLayout там не «плохо
  выглядит», а падает при инфляции. Разрешены FrameLayout/LinearLayout/RelativeLayout/
  GridLayout и коллекции (ListView/GridView/StackView/AdapterViewFlipper).

  ⚠️ `w_empty` обязан быть УКАЗАН через setEmptyView: без него пустой список выглядит
  как «виджет сломался», а не как «пар нет».
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="12dp">

    <!-- Шапка: день + чётность недели -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/w_day"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="17sp"
                android:textStyle="bold"
                android:textColor="@color/gb_widget_text"
                android:text="Сегодня, пятница" />

            <TextView
                android:id="@+id/w_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="12sp"
                android:textColor="@color/gb_widget_text2"
                android:text="К74/1" />
        </LinearLayout>

        <TextView
            android:id="@+id/w_week"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="8dp"
            android:background="@drawable/widget_badge_bg"
            android:paddingLeft="9dp"
            android:paddingRight="9dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp"
            android:maxLines="1"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@color/gb_widget_accent"
            android:text="I неделя" />
    </LinearLayout>

    <!-- Прокручиваемый список пар -->
    <ListView
        android:id="@+id/w_rows"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="8dp"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:clipToPadding="false" />

    <TextView
        android:id="@+id/w_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:textSize="14sp"
        android:textColor="@color/gb_widget_text2"
        android:text="@string/widget_no_pairs" />

    <TextView
        android:id="@+id/w_foot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11sp"
        android:textColor="@color/gb_widget_text2"
        android:text="обновлено 18:40" />
</LinearLayout>
