<?xml version="1.0" encoding="utf-8"?>
<!-- Подложка виджета. Скруглeние 20dp — как у карточек SPA (rounded-2xl). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/gb_widget_bg" />
    <corners android:radius="20dp" />
    <stroke android:width="1dp" android:color="@color/gb_widget_border" />
</shape>
