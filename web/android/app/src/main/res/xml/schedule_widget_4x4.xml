<?xml version="1.0" encoding="utf-8"?>
<!--
  4×4 — весь учебный день. Прошедшие пары ПОКАЗЫВАЕМ приглушённо: они дают опору
  «сейчас третья из пяти», а без них виджет к вечеру ещё и полупустой. Список
  прокручивается, поэтому длинный день влезает целиком независимо от кегля.

  Подробности про три пункта в списке добавления и про updatePeriodMillis — в
  schedule_widget_2x2.xml, дублировать здесь не стали.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:minWidth="180dp"
    android:minHeight="180dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="500dp"
    android:targetCellWidth="4"
    android:targetCellHeight="4"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/widget_schedule_list"
    android:previewLayout="@layout/widget_schedule_list"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/widget_schedule_description_4x4"
    tools:targetApi="s" />
