"""
defaults.py — справочные значения по умолчанию для ПУСТОЙ базы.

━━ ЗАЧЕМ ОТДЕЛЬНЫЙ МОДУЛЬ ━━
Эти два списка — ДАННЫЕ предметной области (какие группы и предметы показать, пока
администратор не завёл свои), а лежали они в `ui/styles.py` — модуле оформления, рядом
с палитрой и таблицами стилей Qt. Из-за этого слой данных импортировал слой интерфейса:
`data/utils.py` начинался строкой `from styles import DEFAULT_GROUPS, DEFAULT_SUBJECTS`.

Направление зависимости было перевёрнуто: интерфейс вправе знать о данных, данные об
интерфейсе — нет. Практическая цена такой инверсии не теоретическая: любой, кто захочет
переиспользовать `data/` без Qt (а именно это и делает серверная сторона проекта, и это
же нужно для тестов без экрана), потянул бы за собой `ui/styles.py` целиком.

Модуль намеренно пустой по зависимостям — чистый stdlib, ничего не импортирует вовсе.

⚠️ Это НЕ «настройки продукта» и не место для новых параметров. Сюда попадает только то,
что показывается человеку в первый запуск на пустой базе и немедленно перекрывается
реальными данными из справочника.
"""

#Группы «по умолчанию» — те же, что исторически лежали в styles.py. В нижнем регистре
#намеренно: так они и были заведены, а сравнение групп в продукте регистронезависимое.
DEFAULT_GROUPS = ["к74/1", "к74/2", "к74/3", "к75/0"]

#Предметы «по умолчанию» — специальность 09.02.07, ядро первого курса. Живой список
#приезжает импортом учебного плана с esstu.ru; этот нужен, чтобы пустой журнал не
#выглядел сломанным до первого импорта.
DEFAULT_SUBJECTS = [
    "Компьютерные сети",
    "Основы алгоритмизации и программирования",
    "Основы проектирования баз данных",
    "Разработка программных модулей",
]
