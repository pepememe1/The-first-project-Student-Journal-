================================================================================
 GradeBookAI — ПЛАН: ПРОДАЖА ВСГУТУ, РЕЕСТР МИНЦИФРЫ, ПЕРЕНОС СЕРВЕРА
 Составлен 29.08.2026. Ведёт: Ярослав. Формат — чек-лист, а не описание.
 Правило: пункт закрывается ФАКТОМ (проверил, получил бумагу, снял замер),
 а не намерением. Устаревший план хуже отсутствующего.
================================================================================

--------------------------------------------------------------------------------
 0. ЧТО УЖЕ ПРАВДА НА СЕГОДНЯ (не переспрашивать, проверено)
--------------------------------------------------------------------------------
 [x] Оператор ПДн — ВСГУТУ. Основание: у вуза УЖЕ есть утверждённая политика
     (приказ от 06.04.2026 № 915од), студенты в Приложении № 1 заявлены как
     категория субъектов. Мы этого не решали — мы это обнаружили.
 [x] Наш privacy.html — ПРИЛОЖЕНИЕ к их политике, описывает одну ИСПДн.
     Вторая политика на те же данные разошлась бы с первой.
 [x] Трансграничной передачи из кода нет: перевод переведён на локальный Argos,
     deep-translator снесён и из репозитория, и из venv на бою.
 [x] Продаём БЕССРОЧНУЮ ПРОСТУЮ ЛИЦЕНЗИЮ (ст. 1235, 1286 ГК), не отчуждение.
 [x] Роспатент — заявка подана, ждём. Реестр Минцифры — после неё.

--------------------------------------------------------------------------------
 1. ЖЁСТКИЕ ОГРАНИЧЕНИЯ ИЗ ИХ ЖЕ ПОЛИТИКИ (это не пожелания)
--------------------------------------------------------------------------------
 п. 5.2.4.1 «ВСГУТУ не поручает обработку персональных данных другому лицу.»
   -> Сервер ОБЯЗАН стоять на их железе. Пока он у нас на reg.ru, вуз
      прямо сейчас не соответствует собственной политике.
   -> Любой наш доступ к боевой базе (даже «разово, посмотреть») требует
      договора по ч. 3 ст. 6 152-ФЗ И правки их политики приказом.
      Сопровождение планировать ИСХОДЯ ИЗ ЭТОГО, а не вопреки.

 п. 5.6.1 «Трансграничная передача персональных данных не осуществляется.»
   -> Под этим пунктом висят Klipy (GIF) и Open-Meteo (погода) — раздел 4.

--------------------------------------------------------------------------------
 2. ПОДГОТОВКА СЕРВЕРА К ПЕРЕНОСУ НА ЖЕЛЕЗО ВСГУТУ
--------------------------------------------------------------------------------
 2.1 ЧТО СПРОСИТЬ У ИХ АДМИНИСТРАТОРОВ ДО ЛЮБЫХ РАБОТ (одним письмом)
     [ ] ОС и версия. Нужен Linux: на Windows не встанет sqlcipher3, то есть
         шифрование базы целиком (ПДн at rest) работать не будет;
     [ ] сколько ядер / RAM / диска дают. От этого зависит, включаем ли
         Whisper (STT), Silero (TTS) и Argos (перевод) — на нынешнем VPS
         (1 ядро, 960 МБ) они не помещаются, замеры сделаны и записаны;
     [ ] есть ли ПУБЛИЧНЫЙ адрес и открыты ли 80/443 наружу.
         ЭТО ГЛАВНЫЙ БЛОКЕР И ОН НЕ НАШ. Без него телефоны студентов
         из мобильной сети до журнала не достучатся;
     [ ] выпускает ли вуз TLS-сертификат сам или можно Let's Encrypt;
     [ ] кто заводит DNS-запись и на какое имя;
     [ ] стоит ли сертифицированное СКЗИ (ViPNet CSP / КриптоПро CSP).
         Если да — ГОСТ-этап хеша и ГОСТ-TLS идут через него,
         см. DEPLOY-VSGUTU-SECURE.md; дописывать код не нужно;
     [ ] кто получит root и кто дежурит (мы доступа не имеем, см. 5.2.4.1);
     [ ] регламент резервного копирования — их или наш, но ОДИН.

 2.2 ЧТО ДЕЛАЕМ МЫ ДО ПЕРЕЕЗДА (инженерное, см. docs/PLAN-HARDENING.txt)
     [x] CI зелёный (29.08.2026). Был КРАСНЫМ на всех 23 прогонах, которые
         отдаёт история — то есть гейт существовал, в него верили, и он не
         значил НИЧЕГО. Четыре причины, все настоящие: web/build/
         stripHtmlComments.js не был в git НИ РАЗУ (его глотала строка
         "build/" в .gitignore), а vite.config.js его импортирует — на
         чистом клоне npm run build падал вовсе; requirements не объявлял
         requests/pydantic/starlette/cryptography; ruff давал 1328 ошибок;
         compileall ходил в каталог ui, переименованный 13.08.
     [x] неизменяемый артефакт релиза + деплой ссылкой с откатом
         (29.08.2026): tools/build_release.py — две сборки на одном коммите
         дают ПОБАЙТОВО одинаковый архив (замерено), deploy/deploy-release.sh
         разворачивает в свой каталог и переключает ссылку, откат
         автоматический при неподнявшемся /health.
     [x] секреты в хранилище (29.08.2026): app/secrets_source.py — одна
         точка чтения, порядок «учётные данные systemd -> файл _FILE ->
         переменная окружения». Причина: переменные процесса видны в
         /proc/<pid>/environ и в systemctl show, то есть ключ от базы с ПДн
         читает любой, кто получил root — в том числе администратор вуза.
     [x] SBOM (CycloneDX) — 05.09.2026: tools/build_sbom.py. Состав
         ВЫВОДИТСЯ из объявлений, а не перечисляется руками: рукописный
         список устареет молча, и узнаем мы об этом на приёмке.
         Держит server/tests/test_sbom.py (6 проверок).
     [x] MFA для admin обязательный (29.08.2026): TOTP, server/app/totp.py,
         одна дверь — deps.require_admin, признак боя — config.IS_PROD.
         Пока фактор не пройден, токена НЕ СУЩЕСТВУЕТ (короткий challenge
         с typ=mfa), а не «токен с пометкой».

     ⚠ ВСЕ ПЯТЬ ПУНКТОВ БЫЛИ СДЕЛАНЫ 29.08, А ОТМЕЧЕНЫ 05.09 — неделю план
       показывал незакрытым то, что уже работало. Это ровно та болезнь, от
       которой предостерегает шапка этого же файла («устаревший план хуже
       отсутствующего»): по нему оценивали готовность к переезду и получали
       заниженную картину.

 2.3 ПОРЯДОК САМОГО ПЕРЕЕЗДА (механизм уже написан)
     Раздел «Сервер» В ПРОГРАММЕ (не на сайте) умеет пошаговый перенос:
     8 шагов, копия снимается ДО остановки, старый сервер НЕ удаляется.
     [ ] 1. Снять резервную копию боевой базы (архив = база + .env);
     [ ] 2. Поднять на их машине окружение (Python 3.12+, venv, Caddy);
     [ ] 3. Перенести артефакт релиза и состояние;
     [ ] 4. Проверить, что база открывается ИХ ключом (GRADEBOOK_DB_KEY);
     [ ] 5. Поднять службу, дождаться /health;
     [ ] 6. Проверить снаружи: вход, журнал, расписание, мессенджер, пуши;
     [ ] 7. Переключить DNS — ВРУЧНУЮ, не автоматом;
     [ ] 8. Старый VPS НЕ гасить минимум неделю. Оплачен — пусть стоит;
     [ ] 9. После недели тишины стереть базу и ключи со старого VPS.
            Это требование п. 5.2.4.1, а не гигиена.

 2.4 ЧТО ПОМЕНЯЕТСЯ В КОДЕ ПРИ ПЕРЕЕЗДЕ (список адресов, чтобы не забыть)
     [ ] GRADEBOOK_SITE_URL, GRADEBOOK_WEBAUTHN_RP_ID / _ORIGIN — новый домен.
         ⚠ WebAuthn привязан к домену: ВСЕ существующие passkey перестанут
            работать. Людей предупредить ЗАРАНЕЕ, вход по паролю оставить;
     [ ] адрес синхронизации в десктопе (сохранён у каждого локально) —
         нужен переходный период, когда отвечают ОБА адреса;
     [ ] манифесты /desktop/updates и /app/updates — иначе парк программ и
         телефонов навсегда останется обновляться со старого сервера;
     [ ] Caddyfile: домен, CSP, encode, Cache-Control (все три уже есть);
     [ ] APK: при смене домена нужен перезалив в RuStore, OTA не хватит.

--------------------------------------------------------------------------------
 3. ЧТО НУЖНО ОТ ЮРИСТА (конкретно, по документам)
--------------------------------------------------------------------------------
 3.1 ГЛАВНЫЙ ВОПРОС, ОТ КОТОРОГО ЗАВИСИТ ВСЁ ОСТАЛЬНОЕ
     Мы утверждаем: оператор — ВСГУТУ, разработчик обработку не ведёт и
     поручения не принимает. Юрист должен это ПОДТВЕРДИТЬ ПИСЬМЕННО либо
     опровергнуть. Опровергнет — переписывать придётся privacy.html,
     terms.html, договор и схему хостинга разом.

 3.2 web/public/privacy.html — заполнить помеченные поля
     [ ] полное наименование и реквизиты оператора (ВСГУТУ): ОГРН, ИНН,
         юридический адрес, адрес для обращений субъектов;
     [ ] ФИО и должность лица, ответственного за организацию обработки ПДн
         (ст. 22.1 152-ФЗ), и его контакт;
     [ ] реквизиты акта об уровне защищённости ИСПДн (ПП РФ № 1119):
         какой уровень присвоен и каким документом;
     [ ] срок хранения журнала аудита и журнала событий безопасности;
     [ ] канал и срок ответа на обращение субъекта (закон — 10 рабочих
         дней, ст. 20 152-ФЗ).

 3.3 web/public/terms.html — заполнить и проверить
     [ ] реквизиты правообладателя (нас): ИП/ООО, ОГРН(ИП), ИНН, адрес;
     [ ] ⚠ СРОК, ТЕРРИТОРИЯ И ВИД ЛИЦЕНЗИИ — ПИСАТЬ СЛОВАМИ. По ст. 1235 ГК
         умолчания против нас: нет срока -> пять лет, нет территории ->
         только РФ. Нам нужно «бессрочно» явным текстом;
     [ ] порядок технической поддержки и её границы;
     [ ] ответственность и её пределы (покупатель — бюджетное учреждение,
         их юристы это будут править, готовимся);
     [ ] подсудность.

 3.4 ЛИЦЕНЗИОННЫЙ ДОГОВОР С ВСГУТУ (отдельный документ, его ещё нет)
     [ ] простая (неисключительная) лицензия, БЕССРОЧНО, территория РФ;
     [ ] предмет — право использования программы. НЕ отчуждение
         исключительного права и НЕ поручение обработки ПДн;
     [ ] пункт «Лицензиар не получает доступ к персональным данным,
         обрабатываемым Лицензиатом» — он же защищает нас от их 5.2.4.1;
     [ ] если сопровождение всё-таки потребует доступа к базе — ОТДЕЛЬНОЕ
         соглашение о поручении обработки (ч. 3 ст. 6 152-ФЗ). Иначе мы
         своими руками вводим вуз в нарушение его же политики;
     [ ] передача исходников / депонирование — решить ДО подписания;
     [ ] НДС: без реестра Минцифры к цене добавляется 20 %
         (пп. 26 п. 2 ст. 149 НК). Цену обсуждать, зная это.

 3.5 ЧТО СПРОСИТЬ У ИХ ОТВЕТСТВЕННОГО ЗА ПДн (это вуз, а не наш юрист)
     [ ] подтверждение, что наша ИСПДн вписывается в их политику и
         Приложение № 1, либо что нужна правка приказом;
     [ ] письменная квалификация Klipy и Open-Meteo (раздел 4);
     [ ] уведомлён ли Роскомнадзор и надо ли дополнять уведомление;
     [ ] сроки и адресаты уведомления об инциденте — заполнить поля
         [ЗАПОЛНИТЬ] в docs/INCIDENT-RESPONSE.md. Уведомляет РКН ОПЕРАТОР
         (вуз), у него на это 24 часа с момента выявления и 72 на итоги
         расследования (ч. 3.1 ст. 21 152-ФЗ). Наш срок — сообщить им в
         течение 4 часов, иначе мы съедаем их законное время.

     ПОДГОТОВЛЕНО К ЭТОМУ РАЗГОВОРУ (05.09.2026):
     [x] docs/SUBPROCESSORS.md — реестр субобработчиков: кому, что именно,
         под какой юрисдикцией и как выключается. Не текст на память:
         server/tests/test_subprocessors.py вычитывает из кода КАЖДЫЙ
         внешний адрес и требует, чтобы он был в реестре описан. Новый
         сервис, добавленный молча, красит прогон.
         ⚠ Граница названа прямо: проверка видит адреса, записанные в коде
           ЛИТЕРАЛОМ. Хост из настройки (S3) или спрятанный в чужой
           библиотеке (GigaChat SDK) она не найдёт — такие строки в реестре
           стоят потому, что мы про них знаем.
     [x] docs/INCIDENT-RESPONSE.md — регламент реагирования. Построен на
         том, что доступа к их машине у нас НЕ БУДЕТ: администратор вуза
         собирает доказательства сам, тремя командами, ДО перезапуска
         службы (рестарт стирает бан приманок — он живёт в памяти процесса).

--------------------------------------------------------------------------------
 4. KLIPY И OPEN-METEO — ЧТО ОСТАЛОСЬ
--------------------------------------------------------------------------------
 Оба иностранные, оба попадают под их п. 5.6.1. Наш довод — «ПДн туда не
 уходят» — должен быть ПОДТВЕРЖДЁН ИХ ОТВЕТСТВЕННЫМ ПИСЬМЕННО.
 Наше собственное мнение здесь ничего не решает.

 4.1 Klipy — поиск GIF (server/app/gif_service.py)
     Уходит: строка поиска, введённая человеком, и служебные параметры.
     Ключ живёт только на сервере, клиент его не видит.
     Риск называем честно: строку пишет ЧЕЛОВЕК и может ввести туда что
     угодно, включая фамилию. Это не передача ПДн оператором, но объяснять
     это придётся словами, а не ссылкой на код.
     [ ] получить письменную квалификацию;
     [ ] ответ «нельзя» — выключается ОДНИМ ключом: пустой
         GRADEBOOK_KLIPY_API_KEY, и пикер просто не показывается.
         Отдельной работы не требует, потеря косметическая.

 4.2 Open-Meteo — погода в приветствии Вектора (weather.py)
     Уходит: координаты Улан-Удэ, зашитые в код. Ни одного поля
     пользователя; запрос одинаков независимо от того, кто спросил.
     Самый защитимый из двух случаев.
     [ ] получить письменную квалификацию;
     [x] ответ «нельзя» — гасится ОДНОЙ переменной: GRADEBOOK_WEATHER=off
         (05.09.2026). Реплика про погоду просто не появляется: все
         вызывающие уже умеют обрабатывать «не знаю», отдельного состояния
         «выключено» заводить не пришлось.
         ⚠ ЗДЕСЬ СТОЯЛО «убрать вызов» — то есть юридический отказ требовал
           бы ПРАВКИ КОДА, сборки и выкладки НА ЧУЖОЙ МАШИНЕ, к которой у
           нас по п. 5.2.4.1 доступа не будет. Настройка, которой нет,
           превращает пятиминутный разговор в релиз. Держит
           tests/test_weather_kill_switch.py (4, с обратным ходом:
           проверяется, что наружу не ушёл НИ ОДИН запрос, а не что функция
           вернула пустоту — последнее зелено и при выключенном интернете).
     [ ] запасной путь на будущее: российский источник (Гидрометцентр,
         Яндекс.Погода по договору). Делать заранее смысла нет.

 4.3 Третий кандидат, о котором нельзя забыть
     [ ] GigaChat (Сбер) — РОССИЙСКИЙ, трансграничности нет. Но туда
         уходит ТЕКСТ вопроса к Вектору. ФИО маскируются, факты берутся
         SQL-ом — и всё равно его надо вписать в перечень привлекаемых
         лиц и показать юристу. Ollama локальная, вопросов не вызывает.

--------------------------------------------------------------------------------
 5. РЕЕСТР МИНЦИФРЫ (ПП РФ № 1236) — ЧТО ПОНАДОБИТСЯ
--------------------------------------------------------------------------------
 Зачем: госзаказчик обязан закупать из реестра, иначе нужно обоснование
 невозможности. И НДС — льгота действует ТОЛЬКО для ПО из реестра.
     [ ] свидетельство Роспатента (ст. 1262 ГК) — ждём;
     [ ] правообладатель — гражданин РФ либо российское юрлицо
         с долей российского участия свыше 50 %;
     [ ] подтверждение, что выплаты иностранным правообладателям за год
         составляют менее 30 % выручки от реализации ПО;
     [ ] документация: описание функциональных характеристик, инструкция
         по установке, инструкция пользователя, описание процессов
         поддержки и жизненного цикла;
     [ ] отсутствие принудительной зависимости от иностранного ПО.
         ⚠ Сюда смотрит вопрос Klipy и вообще иностранных сервисов.
         Переход на локальный Argos играет ЗДЕСЬ ЖЕ, не только в 152-ФЗ;
     [ ] экземпляр ПО, доступный проверяющему (демо-стенд или сборка);
     [x] состав зависимостей / SBOM — 05.09.2026, tools/build_sbom.py
         (CycloneDX 1.5). Запуск: python -X utf8 tools/build_sbom.py --out sbom.json
         На БОЕВОЙ машине добавить --installed: тогда в отчёт попадут реальные
         версии и лицензии установленных пакетов. Без него документ честно
         пишет в поле notes, что версии не проставлены, — выдавать
         ограничения («>=2.0») за установленные версии нельзя.

--------------------------------------------------------------------------------
 6. ПОРЯДОК ДЕЙСТВИЙ
--------------------------------------------------------------------------------
   1. Инженерная часть — CI, артефакт, секреты, SBOM, MFA (наш заход).
   2. Письмо их администраторам (п. 2.1) — параллельно, ответа ждать долго.
   3. Юрист — раздел 3. Начинать ДО Роспатента: это самый длинный путь.
   4. Роспатент принят -> подача в реестр Минцифры (раздел 5).
   5. Договор подписан -> переезд (п. 2.3).
   6. Неделя наблюдения -> зачистка старого VPS (п. 2.3.9).

 ⚠ ЧЕГО НЕ ДЕЛАТЬ: не связывать переезд с релизом. Переезжает то, что уже
   работает и проверено. Выложить новое в день переезда — это два
   независимых источника отказа в одном окне, и разделить их будет нечем.
================================================================================
