# Развёртывание на боевом ПК ВСГУТУ с сертифицированным СКЗИ (ViPNet) — «из коробки»

> Цель: на серверном Linux-ПК колледжа вся криптография (хеши паролей, шифрование ПДн,
> TLS-канал) идёт через **сертифицированное СКЗИ ViPNet CSP** и алгоритмы **ГОСТ** —
> чтобы соответствовать 152-ФЗ/ФСБ/ФСТЭК без вопросов к нам. Код к этому уже готов:
> `server/app/gost.py` и `security.py` САМИ определяют сертифицированный движок и
> используют его, если он установлен.

## 0. Что уже сделано в коде (ничего дополнительно программировать не нужно)
- **Пароли** — гибрид PBKDF2-SHA512 + **Стрибог** (Р 50.1.111-2016). `security.py` авто-берёт
  ГОСТ-бэкенд: OpenSSL GOST-engine/ViPNet (бой) → gostcrypto (dev).
- **Шифрование ПДн** — `gost.py`: «Кузнечик» (ГОСТ 34.12-2018) + Стрибог. Авто-детект движка,
  `backend_info()['certified']` = True при ViPNet.
- Без ключей шифрование = passthrough (прод не падает) — включается заданием ключей.

## 1. Установить ViPNet CSP для Linux (или КриптоПро CSP)
> На ПК разработчика обычно стоит **ViPNet Client** (сетевой) — серверу нужен именно
> **ViPNet CSP** (криптопровайдер) с лицензией ИнфоТеКС.
1. Установить пакет ViPNet CSP для вашего дистрибутива (deb/rpm), активировать лицензию.
2. Подключить **OpenSSL GOST-engine** от ViPNet, чтобы Python (`hashlib`) видел ГОСТ:
   - в `openssl.cnf` включить engine `gost`;
   - проверка: `python3 -c "import hashlib; print('streebog512' in hashlib.algorithms_available)"` → `True`.
3. После этого `gost.backend_info()` вернёт `certified: True` (при `GRADEBOOK_VIPNET=1`).

## 2. Ключи шифрования — через СКЗИ/токен, НЕ plaintext
- Боевые ключи (`GRADEBOOK_DATA_KEY`, `GRADEBOOK_INDEX_KEY`) хранить в хранилище ViPNet /
  на аппаратном токене (**Рутокен ЭЦП 2.0/3.0** или **JaCarta**), а не в `.env`.
- Вести **Журнал учёта СКЗИ и ключей** (Приказ ФСБ №378): выдача, установка, уничтожение.
- В `.env` держать только `GRADEBOOK_VIPNET=1` и путь/идентификатор ключа в СКЗИ.

## 3. Переменные окружения на боевом сервере (`server/.env`, права 600)
```
GRADEBOOK_VIPNET=1
GRADEBOOK_JWT_SECRET=<длинная случайная строка>
GRADEBOOK_ALLOWED_ORIGINS=https://<домен колледжа>
GRADEBOOK_HOST_DEVICE_ID=<uuid хоста>
# Ключи ПДн — из хранилища СКЗИ/токена (не хардкодить):
GRADEBOOK_DATA_KEY=<32 байта hex из СКЗИ>
GRADEBOOK_INDEX_KEY=<ключ слепого индекса>
GRADEBOOK_GOST_ITERS=2000        # на C-скорости ViPNet можно больше
```

## 4. ГОСТ-TLS канала (в дополнение к Let's Encrypt)
- Для передачи ПДн по сети колледжа/интернету поднять **ГОСТ-TLS** через ViPNet Coordinator
  или CSP-совместимый фронт (напр. Stunnel/nginx с ГОСТ-движком) перед приложением.
- Приложение остаётся на 127.0.0.1:8000, а ГОСТ-TLS терминируется на фронте.

## 5. Организационная часть (зона оператора/поставщика — не код)
- Акт классификации ИСПДн → **уровень защищённости** (для колледжа: ПДн «иные», субъектов
  <100k, угрозы 3-го типа → **УЗ-3**; только общедоступные → УЗ-4).
- Класс СКЗИ под УЗ-3/тип-3 — **КС1** (минимально допустимый).
- Модель угроз и нарушителя (ФСТЭК), политика обработки ПДн, согласие субъекта ПДн
  (галка при регистрации), уведомление РКН, назначение ответственного за ИБ.
- **Аттестация ИСПДн** (для госоператора) или оценка соответствия; формуляр СКЗИ,
  акты установки, журнал учёта ключей.

## Итог
Технически система готова принять сертифицированный ГОСТ-контур ViPNet «из коробки»:
поставили ViPNet CSP + GOST-engine, положили ключи в СКЗИ/токен, задали `GRADEBOOK_VIPNET=1`
— и хеши/шифрование/индексы пойдут через сертифицированный ГОСТ без правок кода.
Остальное (аттестация, документы, лицензии) — организационный трек оператора.
