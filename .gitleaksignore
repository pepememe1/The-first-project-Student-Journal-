# Находки gitleaks, разобранные поимённо. Формат строки: коммит:файл:правило:строка.
# ⚠️ Сюда попадает ТОЛЬКО то, что проверено глазами и безопасно, — с причиной рядом.
# Строка без причины — это не исключение, а забытый случай (урок test_spa_fallback).

# Заглушка ключа в тесте инструмента ротации: "0123456789abcdef" * 4. Настоящего ключа
# в строке нет — тест проверяет, что `_pfx` показывает префикс, а не ключ целиком.
# Разобрано 21.09.2026.
4028d18cbeb4eaf1c2fbd67c2eee4d2f7b07dd00:server/tests/test_rotate_db_key.py:generic-api-key:108

# НАСТОЯЩИЙ ключ OpenRouter в GUI.py (коммит 0a74966 — старая история до 25.06.2026, без
# общего корня с нынешней). Десять веток, где он лежал, удалены с GitHub 22.09.2026 (архив:
# WORK/GB-archive/gradebook-old-branches-2026-09-22.bundle), но коммит ещё достижим через
# семь старых тегов (Alpha1.1, Pre-Release1.0, Release, Release_2.1, Release_2.2,
# release_2.3, v2.5), и gitleaks его видит.
# 🔴 СТРОКА НИЖЕ НАМЕРЕННО ЗАКОММЕНТИРОВАНА: исключение вносится ТОЛЬКО ПОСЛЕ отзыва ключа на
# openrouter.ai (отзывает Ярослав, решение 22.09.2026). Раскомментировать и дописать сюда
# «отозван ДД.ММ.ГГГГ» — иначе зелёный гейт прятал бы живую утечку (замечание Полковника).
# 0a749660d4d4d9cbbbdcd7bb64b4456cca16545a:GUI.py:generic-api-key:13
