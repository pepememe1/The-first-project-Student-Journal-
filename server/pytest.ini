[pytest]
# Тесты сервера запускаются из каталога server/. pythonpath=. делает пакет app
# импортируемым (import app.main), как при обычном запуске uvicorn из server/.
pythonpath = .
testpaths = tests
