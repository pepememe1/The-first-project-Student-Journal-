# ============================================================
# server/requirements.txt — Бэкенд GradeBookAI (Фаза 1)
# Python 3.10+
# Установка:  pip install -r server/requirements.txt
# ============================================================
fastapi>=0.110          # веб-фреймворк API
python-multipart>=0.0.9 # загрузка аудио для голосового ввода (/web/vector/stt)
uvicorn[standard]>=0.29 # ASGI-сервер для запуска
SQLAlchemy>=2.0         # ORM (база одна — SQLite, на бою под SQLCipher)
python-jose[cryptography]>=3.3   # JWT-токены
httpx>=0.27             # для тестов (TestClient)
# 🔥 Три пакета ниже импортируются НАПРЯМУЮ и НА УРОВНЕ МОДУЛЯ, а приезжали
# транзитивно — как зависимости fastapi. На машине разработчика и на бою всё
# работало, потому что они стояли случайно; на чистой установке сервер не
# поднимался бы вовсе. Найдено 29.08.2026, когда впервые прочитали, почему красный
# CI. Держит `server/tests/test_requirements_complete.py`.
# ⚠️ Опираться на транзитивную зависимость нельзя: fastapi вправе сменить свою
# внутреннюю кухню в любой версии, и тогда у нас отвалится импорт, которого мы
# ему не заказывали.
requests>=2.31          # парсеры портала/расписания, клиент LLM
pydantic>=2.0           # модели запросов (импортируется в роутерах напрямую)
starlette>=0.36         # Response/StreamingResponse, middleware безопасности
pytest>=8.0             # автотесты API (запуск: cd server && pytest)
# ГОСТ-этап хеша пароля (PBKDF2-Стрибог512). Fallback на dev/без GOST-engine.
# На БОЕВОМ Linux ставь OpenSSL GOST-engine (ViPNet/gost-engine) — бэкенд авто-выберет его.
gostcrypto>=1.2.5
# 🔒 cryptography — не дополнение, а условие работоспособности: ею шифруются ПДн
# (Fernet) и защищаются ключи. Приезжала транзитом через python-jose[cryptography],
# то есть держалась на чужом выборе экстраса. В pyproject объявлена всегда, здесь — нет;
# расхождение двух источников нашлось 29.08.2026 сверкой, а не падением.
cryptography>=42.0
openpyxl>=3.1        # экспорт журнала в xlsx с сайта (/web/teacher/journal.xlsx)
webauthn>=2.0        # passkeys — вход по Face ID/отпечатку без пароля (/auth/webauthn/*)
gigachat>=0.1        # LLM-озвучка Вектора (GigaChat, РФ-облако; VPS в РФ → работает)
# Шифрование файла БД (SQLCipher, AES-256) — ПДн at rest / 152-ФЗ. Только Linux: на
# Windows готового wheel нет, там БД без шифрования файла (dev). Включается ключом
# GRADEBOOK_DB_KEY (см. db.py). Миграция plaintext→encrypted — server/encrypt_db.py.
sqlcipher3-binary>=0.5; sys_platform == "linux"
python-docx>=1.1.0
# ━━ ТЯЖЁЛЫЙ НАБОР (распознавание речи, озвучка, перевод) ЖИВЁТ В ОТДЕЛЬНОМ ФАЙЛЕ ━━
# `server/requirements-ai.txt` — ставится на машине, где для него есть память и ядра:
#     python tools/provision_ai_host.py --apply
# ⚠️ Раньше `faster-whisper` и `argostranslate` лежали ЗДЕСЬ закомментированными, с
# припиской «раскомментировать на машине, где есть память». Так делать нельзя: настройку,
# которую надо ВСПОМНИТЬ и переключить руками, забывают ровно в день переезда — тот же
# класс дефекта, что «проверено полным перебором» в build_nuitka.sh. Теперь пакеты
# объявлены по-настоящему в своём файле, а ставятся одной командой.
# ⚠️ На нынешний VPS этот набор не влезает, и это ИЗМЕРЕНО, а не прикинуто (Argos: +343 МБ
# сверх уже живого torch при 342 МБ свободных). Подробности — в шапке requirements-ai.txt.
# Пока пакетов нет, продукт честно отвечает отказом с причиной, а не молчит.
# Парсер учебных планов ВСГУТУ (server/app/parsers/esstu_parser.py) — учебные планы
# специальностей колледжа на сайте отдаются PDF, а не xlsx. ОПЦИОНАЛЬНО: без пакета
# импорт часов/ЗЕТ при создании группы просто недоступен (пустой список + лог),
# список специальностей (requests + html.parser) читается и без него.
pdfplumber>=0.11
