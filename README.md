<div align="center">

# 📊 GradeBookAI

**Десктопный электронный журнал успеваемости с ИИ-помощником**
*для Технологического колледжа ВСГУТУ (Улан-Удэ)*

[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?logo=python&logoColor=white)](https://www.python.org/)
[![PySide6](https://img.shields.io/badge/GUI-PySide6%20(Qt)-41CD52?logo=qt&logoColor=white)](https://doc.qt.io/qtforpython/)
[![SQLite](https://img.shields.io/badge/Local-SQLite-003B57?logo=sqlite&logoColor=white)](https://www.sqlite.org/)
[![PostgreSQL](https://img.shields.io/badge/Sync-PostgreSQL-4169E1?logo=postgresql&logoColor=white)](https://www.postgresql.org/)
[![Version](https://img.shields.io/badge/release-2.4.2-008080)](CHANGELOG.md)
[![Status](https://img.shields.io/badge/status-active%20development-success)]()

*🏆 Победитель хакатона «Мы — будущее IT Бурятии» (март 2026)*

</div>

---

## О проекте

**GradeBookAI** — это десктопное приложение для ведения электронного журнала успеваемости,
разработанное командой **Synapse**. Главная идея — **local-first**: программа мгновенно работает
офлайн на локальной базе SQLite, а при наличии сети асинхронно синхронизирует данные с общим
сервером PostgreSQL, чтобы все ПК колледжа видели одну базу.

Ключевая фишка — встроенный ИИ-помощник **«Вектор»**, который отвечает на вопросы по журналу,
**никогда не выдумывая цифры**: все данные он берёт реальными SQL-запросами к базе, а облачная
LLM лишь переформулирует уже готовые факты.

> ⚠️ Приложение обрабатывает персональные данные студентов. См. раздел
> [Безопасность и 152-ФЗ](#-безопасность-и-152-фз).

---

## ✨ Ключевые возможности

- **Три роли с разграничением доступа** — Администратор, Преподаватель, Студент. Каждый видит
  только то, что ему положено.
- **Local-first архитектура** — чтение и запись идут в локальный SQLite (без задержек UI),
  фоновый поток синхронизирует данные с PostgreSQL. Полная работоспособность офлайн.
- **ИИ-маскот «Вектор»** — тигр-ассистент с четырьмя состояниями настроения, работает офлайн
  (локальный шаблонизатор) или через GigaChat. Анти-галлюцинации by design.
- **Полевое шифрование** — чувствительные поля шифруются Fernet; мастер-ключ защищён паролем
  администратора (PBKDF2-HMAC-SHA256, 100 000 итераций). Файл `secure_data.enc` переносим между ПК.
- **Хеширование паролей** — пароли никогда не хранятся в открытом виде.
- **Гибкий расчёт среднего балла** — единый источник правды (`grading.py`): учёт пропусков «Н»,
  опциональный учёт экзаменов с пересдачами — всё настраивается вузом.
- **Импорт/экспорт Excel** — выгрузка и загрузка журналов через `.xlsx` (openpyxl).
- **Авто-бэкап** локальной базы при выходе из программы.
- **Фирменный стиль Synapse** — шрифты Syne + DM Sans, бирюзовая палитра ВСГУТУ.

---

## 🐯 ИИ-помощник «Вектор»

«Вектор» — это не «чат-бот поверх ChatGPT», а аккуратный конвейер, где LLM отвечает только
за стиль, но не за факты:

```
вопрос пользователя
   → intents.classify()      распознаём намерение (локально, без сети)
   → intents.<handler>()     реальный SQL к локальной базе → ФАКТЫ
   → privacy.anonymize()     ФИО → «Студент №N» (перед отправкой в облачную LLM)
   → llm.voice()             LLM только ПЕРЕФОРМУЛИРУЕТ факты
   → privacy.deanonymize()   возвращаем настоящие имена локально
   → VectorResponse          текст + настроение + сами факты
```

- **Офлайн-first как преимущество.** Если LLM недоступна — фразы собирает локальный
  шаблонизатор, поэтому Вектор работает всегда.
- **Приватность.** Перед отправкой в облако ФИО обезличиваются — наружу персональные данные
  не уходят.
- **Знания о вузе** (`vector/knowledge.py`) — выверенные факты о ВСГУТУ отвечаются офлайн,
  без LLM, что исключает галлюцинации.
- **Четыре состояния маскота** — `idle` / `thinking` / `speaking` / `away` (арт легко заменить
  на свой, см. `vector_assets/README.md`).

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     GradeBookAI (PySide6)                    │
│                                                              │
│   UI ──► чтение/запись ──► SQLite (локальный, мгновенно)     │
│                              │                               │
│                              ▼  (фоновый поток PGSyncer)     │
│                          PostgreSQL  ◄──► другие ПК колледжа  │
└─────────────────────────────────────────────────────────────┘
```

- **SQLite всегда локальный** — никогда не на сетевой шаре (иначе блокировки и порча файла).
- **Общее состояние между ПК — только через PostgreSQL.**
- При старте данные подтягиваются из PostgreSQL в SQLite; офлайн-изменения досинхронизируются
  при восстановлении сети.
- Данные хранятся в key-value таблице `kv_store` (ключ → JSON): студенты, преподаватели,
  группы, конфиг, журналы.

---

## 🔐 Безопасность и 152-ФЗ

- Поля чувствительных данных шифруются (**Fernet**), мастер-ключ защищён паролем администратора
  через **PBKDF2-HMAC-SHA256** (100 000 итераций).
- Пароли пользователей хранятся только в виде хешей.
- ИИ-конвейер обезличивает ФИО перед отправкой в облачную LLM.
- GigaChat выбран как LLM в РФ-юрисдикции (российские серверы).

> **Важно перед публикацией / продакшеном:**
> 1. В коде есть **дефолтные учётные данные администратора** — обязательно смените их перед
>    реальным использованием.
> 2. Репозиторий рекомендуется держать **приватным**, пока не вынесены секреты и не пройдён
>    аудит на соответствие 152-ФЗ.
> 3. Рантайм-файлы (`pg_config.json`, `secure_data.enc`, `*.db`) исключены из git
>    через `.gitignore` — не коммитьте их.

---

## 🚀 Установка и запуск

> Требуется **Python 3.10+**.

```bash
# 1. Клонировать репозиторий
git clone https://github.com/<ваш-логин>/GradeBookAI.git
cd GradeBookAI/gradebook

# 2. Создать виртуальное окружение
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# (опционально) синхронизация PostgreSQL и ИИ через GigaChat:
pip install psycopg2-binary gigachat

# 4. Запустить
python main.py
```

При первом запуске программа работает на локальном SQLite. Подключение к общему PostgreSQL
настраивается в панели администратора (см. `db_config.py`).

---

## 📁 Структура проекта

```
gradebook/
├── main.py                 # точка входа
├── main_window.py          # главное окно, маршрутизация по ролям
├── core.py                 # DBManager, синхронизация SQLite ↔ PostgreSQL
├── data_store.py           # key-value хранилище (kv_store)
├── db_config.py            # настройка PostgreSQL, ключи установки
├── security.py             # шифрование Fernet, хеширование паролей
├── grading.py              # единый расчёт среднего балла
├── auth_pages.py           # экраны входа
├── dashboards.py           # дашборд студента
├── teacher_dashboard.py    # дашборд преподавателя
├── admin_dashboard.py      # панель администратора
├── subjects.py             # предметы и занятия
├── ai_module.py            # ИИ через OpenRouter
├── vector/                 # ИИ-маскот «Вектор»
│   ├── engine.py           #   движок ответов
│   ├── intents.py          #   распознавание намерений + SQL-факты
│   ├── privacy.py          #   обезличивание ПДн
│   ├── llm.py              #   провайдеры (офлайн-шаблоны / GigaChat)
│   ├── knowledge.py        #   выверенные факты о ВСГУТУ
│   └── ...
├── vector_assets/          # арт маскота (4 состояния)
├── styles.py / fonts.py    # фирменный стиль Synapse
├── requirements.txt
└── CHANGELOG.md
```

---

## 🗺️ Дорожная карта

- [ ] Вынести дефолтные учётные данные из исходников
- [ ] Аудит на соответствие 152-ФЗ (обработка персональных данных)
- [ ] Нагрузочное тестирование PostgreSQL-синхронизации
- [ ] Настроенные регулярные бэкапы
- [ ] Реальный арт маскота вместо заглушек
- [ ] Парсер расписания
- [ ] Подготовка к коммерческому внедрению во ВСГУТУ

Подробная история изменений — в [CHANGELOG.md](gradebook/CHANGELOG.md).

---

## 👥 Команда Synapse

| Участник     | Роль                                   |
|--------------|----------------------------------------|
| **Ярослав**  | Технический лид, продуктовое видение   |
| **Владислав**| Бэкенд                                 |
| **Арина**    | Фронтенд, презентации, арт             |

---

## 📄 Лицензия

Лицензия пока не выбрана. Поскольку проект готовится к коммерческому внедрению,
рекомендуется определиться с лицензией (или оставить проприетарной) до публикации в открытый доступ.

---

<div align="center">

*Сделано командой Synapse · Технологический колледж ВСГУТУ · Улан-Удэ*

</div>
